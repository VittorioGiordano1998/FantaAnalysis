# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# IDE
.idea/
.vscode/
*.iml
.DS_Store

# App data — rigenerati dall'app (cache CSV + stato asta JSON)
data/*
!data/.gitkeep
output/

# Streamlit secrets (mai versionare credenziali)
.streamlit/secrets.toml

# Agent config is single-sourced in .claude/. opencode only scans .opencode/,
# so these two are junctions (Windows) / symlinks (macOS, Linux) into .claude/.
# Recreate after clone: tools/link-agent-config.ps1  (or .sh)
/.opencode/agents/
/.opencode/commands/
