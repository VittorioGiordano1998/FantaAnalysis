[tool.ruff]
line-length = 100
target-version = "py311"
src = ["."]
exclude = ["data", "output", ".venv"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
